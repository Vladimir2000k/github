import styled from 'styled-components';

const MainPagesContentContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  overflow-y: auto;
`;

export default MainPagesContentContainer;
