import styled from 'styled-components';

const LandingPagesContainer = styled.div`
  display: flex;
  flex-direction: column;
  max-width: 1100px;
  margin: 0 auto;
  padding-left: calc(100vw - 100%);
  height: 100%;
`;

export default LandingPagesContainer;
